/**
 * Configuración del encabezado / portada de la página de inicio.
 * headerVariant: "default" | "gradient" | "minimal"
 * - default: diseño actual (fondo blanco, arcos de color)
 * - gradient: fondo con gradiente animado usando los colores del logo
 * - minimal: tercera opción de diseño (reservado para futuras variantes)
 */
window.PORTADA_CONFIG = {
  headerVariant: "gradient"
};
